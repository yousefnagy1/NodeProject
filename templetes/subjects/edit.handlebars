<h1>Edit{{subject.name}}</h1>
<form action='/subject/{{subject._id}}?_method=PUT' method="post">
    <div>
        <label for="">name</label>

        <input type="text" class="form-control" name="name" value="{{subject.name}}"/>
    </div>
    <div>
        <label for="">code</label>
        <input type="text" class="form-control" name="code" value="{{subject.code}}"/>
    </div>
    <div>
        <label for="">department</label>

        <select name="department" id="" class="form-select">

            {{#each departments}}

            <option value="{{_id}}">{{name}}</option>


            {{/each}}
        </select>
    </div>
    <div>
        <button class="btn btn-success" type="submit">Edit subject</button>
    </div>
</form>